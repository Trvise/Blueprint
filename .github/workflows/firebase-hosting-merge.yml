# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      # Inject environment variables from GitHub Secrets


      # Install dependencies
      - run: |
          echo "REACT_APP_FIREBASE_API_KEY=${{ secrets.REACT_APP_FIREBASE_API_KEY }}" >> .env
          echo "REACT_APP_FIREBASE_AUTH_DOMAIN=${{ secrets.REACT_APP_FIREBASE_AUTH_DOMAIN }}" >> .env
          echo "REACT_APP_FIREBASE_PROJECT_ID=${{ secrets.REACT_APP_FIREBASE_PROJECT_ID }}" >> .env
          echo "REACT_APP_FIREBASE_STORAGE_BUCKET=${{ secrets.REACT_APP_FIREBASE_STORAGE_BUCKET }}" >> .env
          echo "REACT_APP_FIREBASE_MESSAGING_SENDER_ID=${{ secrets.REACT_APP_FIREBASE_MESSAGING_SENDER_ID }}" >> .env
          echo "REACT_APP_FIREBASE_APP_ID=${{ secrets.REACT_APP_FIREBASE_APP_ID }}" >> .env
          echo "REACT_APP_FIREBASE_MEASUREMENT_ID=${{ secrets.REACT_APP_FIREBASE_MEASUREMENT_ID }}" >> .env
          echo "Checking .env variable lengths:"
          echo "API Key Length: ${#REACT_APP_FIREBASE_API_KEY}"
          echo "Auth Domain Length: ${#REACT_APP_FIREBASE_AUTH_DOMAIN}"
          echo "Project ID Length: ${#REACT_APP_FIREBASE_PROJECT_ID}"
          echo "Storage Bucket Length: ${#REACT_APP_FIREBASE_STORAGE_BUCKET}"
          echo "Messaging Sender ID Length: ${#REACT_APP_FIREBASE_MESSAGING_SENDER_ID}"
          echo "App ID Length: ${#REACT_APP_FIREBASE_APP_ID}"
          echo "Measurement ID Length: ${#REACT_APP_FIREBASE_MEASUREMENT_ID}"
          npm install --legacy-peer-deps && npm run build

      # Deploy to Firebase
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_VORTEXLENS }}
          channelId: live
          projectId: vortexlens
